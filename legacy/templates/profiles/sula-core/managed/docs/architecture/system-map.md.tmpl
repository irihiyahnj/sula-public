# {{PROJECT_NAME}} System Map

This profile assumes the repository is itself a reusable project operating system.

## System Boundary

- operating stack: {{FRONTEND_STACK}}
- persistence and rollout metadata: {{BACKEND_STACK}}
- highest rule: `{{HIGHEST_RULE}}`

## Runtime Structure

```mermaid
flowchart TD
    A["Maintainer Or Agent"] --> B["Sula CLI at {{API_LAYER_PATH}}"]
    B --> C["Templates And Managed Docs"]
    B --> D["Registry And Examples"]
    C --> E["Adopted Project Repositories"]
```

## Key Flow Rules

- orchestration entry: [{{API_LAYER_PATH}}](../../{{API_LAYER_PATH}})
- rollout state: [{{STATE_LAYER_PATH}}](../../{{STATE_LAYER_PATH}})
- operator-facing overview: [{{APP_SHELL_PATH}}](../../{{APP_SHELL_PATH}})
- release workflow reference: `{{DEPLOY_WORKFLOW}}`

## Invariants

1. Sula remains a reusable operating system, not a business product.
2. Managed templates must stay portable across adopted projects.
3. Example projects may validate behavior, but they do not become the source of truth for Sula Core.
