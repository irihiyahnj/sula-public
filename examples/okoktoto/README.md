# OKOKTOTO Example

This example shows how Sula represents the current OKOKTOTO v5 repository as a reusable profile consumer.

Key file:

- [`.sula/project.toml`](.sula/project.toml)

The example is intentionally lightweight. The real project keeps its own business truth and change history in its own repository.
