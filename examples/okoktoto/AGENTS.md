# AGENTS.md

This file is the primary instruction source for AI coding agents working in this repository.

If a tool-specific instruction file exists, treat it as a thin adapter to this file.
If any tool-specific file conflicts with this file, `AGENTS.md` wins.

## Repository Identity

- Repository root is `OKOKTOTO v5`.
- Project slug: `okoktoto-v5`.
- Description: Chain coffee and restaurant store management frontend over ERPNext
- Default agent: `Codex`

## Highest Architecture Rule

- `frontend-only orchestration over ERPNext-native capabilities`

## Mandatory Working Rules

- Read this file before making changes.
- GitHub is the durable source of truth.
- Working branches should use `codex/*`.
- Deployment branch is `okoktoto-v5`.
- Keep primary integration logic centralized in [src/api/erpnext.ts](src/api/erpnext.ts).
- Keep app-shell logic centered in [src/App.tsx](src/App.tsx).
- Keep current state in [STATUS.md](STATUS.md).
- Keep change-record rules in [CHANGE-RECORDS.md](CHANGE-RECORDS.md).
- Update `docs/README.md` when adding durable docs.

## Commands

```bash
npm install
npm run dev
npm run build
npx tsc --noEmit
```

## Notes

- This file is a scaffold starter generated by Sula.
- Fill in project-specific hard rules before relying on it as the final repository constitution.
