---
description: {{PROJECT_NAME}} repository rules
globs:
alwaysApply: true
---

Read [AGENTS.md](AGENTS.md) before making changes.

Critical rules:

- Repository root = `{{PROJECT_NAME}}`.
- Highest rule: `{{HIGHEST_RULE}}`
- Keep primary integration logic centralized in [{{API_LAYER_PATH}}]({{API_LAYER_PATH}}).
- Use [docs/README.md](docs/README.md) as the document map and [docs/ops/team-operating-model.md](docs/ops/team-operating-model.md) as the default execution flow.
- If the project enables formal document design rules, follow them for planning, proposal, report, process, and training documents.
- Working branches use `{{WORKING_BRANCH_PREFIX}}*`.
- Deployment branch = `{{DEPLOYMENT_BRANCH}}`.
- Run `{{BUILD_COMMAND}}` before finishing substantial changes.

If this file conflicts with `AGENTS.md`, `AGENTS.md` wins.
