# AGENTS.md

This file is the primary instruction source for AI coding agents working in this repository.

If a tool-specific instruction file exists, treat it as a thin adapter to this file.
If any tool-specific file conflicts with this file, `AGENTS.md` wins.

## Repository Identity

- Repository root is `{{PROJECT_NAME}}`.
- Project slug: `{{PROJECT_SLUG}}`.
- Description: {{PROJECT_DESCRIPTION}}
- Default agent: `{{DEFAULT_AGENT}}`

## Highest Architecture Rule

- `{{HIGHEST_RULE}}`

## Mandatory Working Rules

- Read this file before making changes.
- GitHub is the durable source of truth.
- Working branches should use `{{WORKING_BRANCH_PREFIX}}*`.
- Deployment branch is `{{DEPLOYMENT_BRANCH}}`.
- Keep primary orchestration logic centered in [{{API_LAYER_PATH}}]({{API_LAYER_PATH}}).
- Keep rollout state in [{{STATE_LAYER_PATH}}]({{STATE_LAYER_PATH}}).
- Keep operator-facing overview in [{{APP_SHELL_PATH}}]({{APP_SHELL_PATH}}).
- Keep current state in [{{STATUS_FILE}}]({{STATUS_FILE}}).
- Keep change-record rules in [{{CHANGE_RECORDS_FILE}}]({{CHANGE_RECORDS_FILE}}).
- If work touches `STATUS.md`, `CHANGE-RECORDS.md`, `docs/change-records/*`, `.sula/state/current.md`, `.sula/events/log.jsonl`, or `.sula/memory-digest.md`, finish by running `python3 scripts/sula.py check --project-root .`.
- Treat `SULA CHECK OK` as the completion gate for state-sync work, and prefer rebuilding generated `.sula/*` files through Sula commands instead of hand-editing them.

## Commands

```bash
{{INSTALL_COMMAND}}
{{DEV_COMMAND}}
{{BUILD_COMMAND}}
{{TYPECHECK_COMMAND}}
```
