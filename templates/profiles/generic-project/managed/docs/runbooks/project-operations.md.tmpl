# {{PROJECT_NAME}} Project Operations Runbook

Use this runbook for normal project work under the generic Sula kernel.

## Default Loop

1. inspect the current project state
2. read the most relevant source anchors
3. do the work
4. update traceability for non-trivial changes
5. regenerate recall views when they matter

## Minimum Checks

- `{{INSTALL_COMMAND}}`
- `{{DEV_COMMAND}}`
- `{{BUILD_COMMAND}}`
- `{{TYPECHECK_COMMAND}}`

Commands may be `n/a` until the project defines tighter local automation.

## Guardrail

Do not replace project truth with summaries or caches. `.sula/` exists to organize, not to override.
