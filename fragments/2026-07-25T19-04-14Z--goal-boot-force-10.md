---
id: 2026-07-25T19-04-14Z--goal-boot-force-10
time: 2026-07-25T19:04:14Z
kind: goal
refs: [2026-07-25T18-58-01Z--assessment-v1-1-open-edges]
tags: [v1-1, boot, render]
summary: boot 必须给出全部在force判断与全部未闭合方向，而非最近 10 条
done_when: view_digest 对 judgment 与 direction 不再按 recency 截断；--for-agent 列出全部未被 supersede 的判断（当前 92 条）与全部未闭合方向；evidence 仍按 n 取最近；三条选择器各有测试覆盖；全套测试通过且 doctor exit 0
verifier_ref: shell: python3 -m unittest tools.sula_vector.tests.test_sula_vector && python3 tools/sula_vector/render.py . --view doctor
---
实测：judgment 格 99 条，5 条 principle 单独渲染，94 条非 principle 判断中仅 2 条被 supersede，而 boot 只列 10 条——约 82 条仍在force的判断不出现在 boot 上下文里，最老的可见项是 2026-07-25T15:24:55Z，四月到七月的有效决定全部缺席。

成因是 view_digest 里硬编码的 n=10 同时截断三个格。这违反 B2：决定 render 输出内容的参数不是命名片段；也违反 v1.1 自己的「没有静默丢弃」，且 boot 里那句 '(N superseded judgment(s) hidden)' 把 recency 截断说成 supersession，把原因指错了方向。跨模型交接靠 boot，boot 漏掉 87% 的在force判断，则 switch cost = 0 在旧决定上不成立。

修法取维度而非取参数：每个格的截断规则必须是它自身的语义——judgment 由 supersede 终止，direction 由 closure 终止，evidence 才是由 recency 终止。因此 n 只保留它本来的含义（取多少条最近证据），另两处的 [-n:] 删除。不引入可配置项，否则只是把隐式状态换成一个旋钮。
