---
id: 2026-09-05T07-34-08.690980Z--assessment-619af43d-91be7997acc947dab8ee49c3f9004b35
time: 2026-09-05T07:34:08.690980Z
kind: assessment
refs: ["2026-09-05T07-29-42.361671Z--decision-7708762b-645c5b307a1d4924b74bcfb1cdba8aba"]
summary: 最终复核暂不通过：两处新旧捕获兼容缺口
governs: tools/sula_vector/capture.py
---
本轮最终复核只读实现，未改代码。发现两处来自前轮实现、现有测试未覆盖的兼容缺口，均已用内存 Fragment 样例复现，暂不建议合并。

P1：capture_graph 的 previous 同时遍历旧捕获和 capture_format=2 新捕获。旧捕获 A 时间 10:00，新捕获 B 显式以 A 为父但设备时钟为 09:59，按时间排序 B 在 A 前；函数给旧 A 隐式加上 B 为父，造出 A↔B 假环，doctor 报 capture-ancestry，witness 连 --reconcile 也会拒绝。建议只在旧格式捕获之间推导隐式前驱，新格式严格采用声明的父边。补混合旧/新格式并发生时钟回拨的回归，确保重放 A 后 B。

P2：_decode_path 看到双引号就尝试 JSON，却不区分捕获格式。旧写入器对双引号不转义，因此旧 delta 行 + abc 1 "file.txt" 表示文件名本身含两侧双引号；新解析把它变成 file.txt。建议由捕获格式选择路径解码，新格式解析 JSON，旧格式维持旧转义规则；fold_witnessed 与 render.witnessed_paths 两条读取路径都要一致。补旧引号文件名和新 JSON 文件名回归，不重写历史片段。

接手模型仅修这两项并补对应测试、执行受影响测试及既有全链路验收，继续保留工作区现有修改，不扩功能、不合并发布，完成后再交本模型复核。用户提供的测试全绿与结构检查并不能排除上述已复现的边界问题。
